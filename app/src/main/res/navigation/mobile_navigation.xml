<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_main_screen">

    <fragment
        android:id="@+id/nav_main_screen"
        android:name="by.tska.courseapp.ui.MainScreenActivity"
        android:label="@string/menu_home"
        tools:layout="@layout/activity_main_screen" />

    <fragment
        android:id="@+id/nav_addresses"
        android:name="by.tska.courseapp.ui.AddressesFragment"
        android:label="Addresses"
        tools:layout="@layout/fragment_addresses" />

    <fragment
        android:id="@+id/nav_wallets"
        android:name="by.tska.courseapp.ui.WalletsFragment"
        android:label="Wallets"
        tools:layout="@layout/fragment_wallets" />

    <fragment
        android:id="@+id/nav_subscriptions"
        android:name="by.tska.courseapp.ui.SubscriptionsFragment"
        android:label="Subscription"
        tools:layout="@layout/fragment_subscription" />
</navigation>